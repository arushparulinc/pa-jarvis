[project]
name = "pa-jarvis-agents"
version = "0.1.0"
description = "Master-agent HTTP microservice for PA Jarvis"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.139.0",
    "httpx>=0.28.1",
    "python-dotenv>=1.2.2",
    "uvicorn>=0.51.0",
]

[tool.uv]
package = false
