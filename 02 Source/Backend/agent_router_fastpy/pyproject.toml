[project]
name = "pa-jarvis-agent-router"
version = "0.1.0"
description = "HTTP API that routes PA Jarvis chat requests to the master agent"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.139.0",
    "httpx>=0.28.1",
    "pydantic>=2.13.4",
    "python-dotenv>=1.2.2",
    "sqlalchemy>=2.0.51",
    "uvicorn>=0.51.0",
]
