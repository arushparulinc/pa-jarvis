name: pa-jarvis-storage

services:
  storage:
    build:
      context: .
    ports:
      - "8004:8004"
    env_file:
      - .env
    networks:
      - pa-jarvis

networks:
  pa-jarvis:
    name: pa-jarvis-dev
