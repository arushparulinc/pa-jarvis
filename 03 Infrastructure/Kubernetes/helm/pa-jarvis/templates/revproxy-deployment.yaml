apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: revproxy
spec:
  ingressClassName: traefik
  rules:
    - host: chat.arushparulinc.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80