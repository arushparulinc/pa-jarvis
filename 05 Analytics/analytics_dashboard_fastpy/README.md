# Analytics service

Deployable FastAPI service for the PA Jarvis analytics dashboard. The server-rendered Jinja UI will be added separately.

Run locally with `uv sync --frozen` and `uv run uvicorn app.main:app --host 0.0.0.0 --port 8006`.
